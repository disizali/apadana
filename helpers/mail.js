export default class mailDecorator {
  constructor(type, information) {
    this.type = type;
    this.information = information;
  }
  decorateHtml() {
    switch (this.type) {
      case "develop":
    }
  }
  createBasicPage() {}
  appendStyle() {}
}
